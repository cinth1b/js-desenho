function setup() {
    createCanvas(400, 400);
    background ("red");
  }
  
  function draw() {
    stroke ("green");
    fill ("blue");
  if (mouseIsPressed) {
    rect(mouseX.mouseX. 20 . 45)
  }